import { styled } from '@material-ui/styles'
import { Button } from '@material-ui/core'

const StyledButton = styled(Button )({
  background: 'linear-gradient(90deg, #3A506B -50%, #5BC0BE 100%)',
  borderRadius: 10,
  color: '#0B132B',
  padding: '15px',
  marginTop: '3px',
  marginBottom: '3px',
})

export default StyledButton